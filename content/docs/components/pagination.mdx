---
title: Pagination
description: Current page gets the stamp-fill treatment (solid primary, hard shadow). Prev/Next use two-ink chevrons. Ellipsis collapses distant pages.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer"
import { BasicPaginationDemo, SiblingsPaginationDemo } from "@/components/demos/pagination-demos"

<PreviewContainer
  componentName="pagination"
  code={`import { Pagination } from '@/registry/new-york/ui/pagination'

const [page, setPage] = React.useState(3)

<Pagination
  total={247}
  page={page}
  perPage={10}
  onChange={setPage}
/>`}
>
  <BasicPaginationDemo />
</PreviewContainer>

## Installation

```bash
npx shadcn add https://riso.dev/r/pagination.json
```

## More siblings

<PreviewContainer
  componentName="pagination"
  code={`// siblings controls how many pages appear around the current page
<Pagination
  total={500}
  page={page}
  perPage={10}
  onChange={setPage}
  siblings={2}
/>`}
>
  <SiblingsPaginationDemo />
</PreviewContainer>

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `total` | `number` | — | Total item count |
| `page` | `number` | — | Current page (1-indexed) |
| `perPage` | `number` | `10` | Items per page |
| `onChange` | `(page: number) => void` | — | Page change handler |
| `siblings` | `number` | `1` | Pages shown around current |

## Design notes

Current page: solid `var(--riso-primary)` fill with `drop-shadow(2px 2px 0 var(--riso-secondary))` — the small stamp effect. Other pages: transparent background, primary text, light primary outline.

Prev/Next chevrons: the same four-line SVG pattern as Accordion and Select — primary 2px arms + secondary 1px offset arms at 60% opacity.

Ellipsis: `···` in secondary color. The `siblings` prop controls how many pages appear around the current page before collapsing.
